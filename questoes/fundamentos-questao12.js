function unirComSustenido(lista) {
    return lista.join('#');
}

// Exemplo de uso:
const elementos = ['maçã', 'banana', 'laranja', 'uva'];
console.log(unirComSustenido(elementos));  // Saída: "maçã#banana#laranja#uva"